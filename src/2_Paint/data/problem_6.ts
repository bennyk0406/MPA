const colors = ["#FF0000", "#FFC000", "#FFFF00", "#92D050", "#00B0F0", "#9943D9"]
const [R, O, _Y, G, _B, _P] = colors // Please insert underbar(_) in front of the variables which are not used.
const _ = ""

const map = [
    [R, _, G, _],
    [R, _, G, _],
    [O, O, _, O],
    [R, _, G, _]
]

export { colors, map }